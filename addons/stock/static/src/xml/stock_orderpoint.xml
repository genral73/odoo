<?xml version="1.0" encoding="utf-8"?>
<templates id="template" xml:space="preserve">
    <div t-name="stock.leadDaysPopOver">
        <p>
            The forecasted stock <t t-if="virtual">today</t><t t-else="">on the <t t-esc="lead_days_date"/></t>
            is <t t-esc="qty_forecast"/> <t t-esc="product_uom_name"/>.<t t-if="qty_to_order > 0"> As it is below the minimum inventory of <t t-esc="product_min_qty"/> <t t-esc="product_uom_name"/>
            : <t t-esc="qty_to_order"/> <t t-esc="product_uom_name"/> should be replenished to reach the maximum of 
            <t t-esc="product_max_qty"/> <t t-esc="product_uom_name"/>.</t>
            <t t-if="lead_days_description">The forecasted date is computed as</t>
        </p>
        <table t-if="lead_days_description" class="table table-borderless">
            <tbody>
                <tr>
                    <td>
                        Today
                    </td>
                    <td class="float-right">
                        <t t-esc="today"/>
                    </td>
                </tr>
                <t t-raw="lead_days_description"/>
                <tr class="table-info">
                    <td>
                        Forecasted Date
                    </td>
                    <td class="float-right">
                        = <t t-esc="lead_days_date"/>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>

    <t t-name="StockOrderpoint.Buttons">
        <span>
            <button type="button" class="btn btn-secondary o_button_order">
                Order
            </button>
        </span>
    </t>

</templates>
